# Template only — provision real values via your secrets manager
# (External Secrets Operator, Sealed Secrets, or `kubectl create secret`).
apiVersion: v1
kind: Secret
metadata:
  name: kimbal-api-secrets
  namespace: kimbal
type: Opaque
stringData:
  APP_SECRET_KEY: "REPLACE_ME"
  DATABASE_URL: "postgresql+asyncpg://kimbal:REPLACE_ME@postgres.kimbal.svc:5432/kimbal"
  REDIS_URL: "redis://redis.kimbal.svc:6379/0"
  OPENROUTER_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
