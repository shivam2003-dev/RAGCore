# Template only — provision real values via your secrets manager
# (External Secrets Operator, Sealed Secrets, or `kubectl create secret`).
apiVersion: v1
kind: Secret
metadata:
  name: cvum-api-secrets
  namespace: cvum
type: Opaque
stringData:
  APP_SECRET_KEY: "REPLACE_ME"
  DATABASE_URL: "postgresql+asyncpg://cvum:REPLACE_ME@postgres.cvum.svc:5432/cvum"
  REDIS_URL: "redis://redis.cvum.svc:6379/0"
  OPENROUTER_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
