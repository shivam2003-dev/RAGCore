apiVersion: apps/v1
kind: Deployment
metadata:
  name: cvum-api
  namespace: cvum
  labels:
    app: cvum-api
spec:
  replicas: 3
  strategy:
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: cvum-api
  template:
    metadata:
      labels:
        app: cvum-api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: api
          image: ghcr.io/shivam2003-dev/ragcore-api:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: cvum-api-config
            - secretRef:
                name: cvum-api-secrets
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false  # uploads dir; move to object storage to enable
      initContainers:
        - name: migrate
          image: ghcr.io/shivam2003-dev/ragcore-api:latest
          command: ["alembic", "upgrade", "head"]
          envFrom:
            - secretRef:
                name: cvum-api-secrets
